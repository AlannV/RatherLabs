import React from "react";
import logo from "../Images/rather-labs-logo.png";

function Header() {
  return (
    <header className="App-header">
      <img src={logo} alt="rather labs logo" className="rather-labs-logo" />
    </header>
  );
}

export default Header;
